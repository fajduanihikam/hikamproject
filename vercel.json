{
  "version": 2,
  "builds": [
    {
      "src": "hikamproject/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.9",
        "installCommand": "pip3 install -r requirements.txt",
        "buildCommand": "python3.9 manage.py collectstatic --noinput"
      }
    },
    {
      "src": "build_files.sh",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "staticfiles_build"
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "hikamproject/wsgi.py"
    }
  ]
}